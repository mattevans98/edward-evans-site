{
  "repository":  "https://github.com/mattevans98/high-order-bit-site",
  "website": "https://www.high-orderbit.com",
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "yarn install --frozen-lockfile & apt-get update -y & apt-get install libgtk2.0-0 libgtk-3-0 libnotify-dev libgconf-2-4 libnss3 libxss1 libasound2 libxtst6 xauth xvfb",
        "test": "yarn test"
      }
    }
  },
  "buildpacks": [
    {
      "url": "mars/create-react-app"
    }
  ]
}
